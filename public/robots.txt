# Five Minutes with Bob
User-agent: *

# Search result pages are thin, endless and near-duplicate. Keeping crawlers out
# of them spends the crawl budget on the mornings themselves.
Disallow: /*?q=
Disallow: /*?sort=

# Nothing to index behind the sign-in.
Disallow: /admin/

# sitemap.php, not sitemap.xml: the .htaccess rewrite is inert on this server
# (AllowOverride is off), so /sitemap.xml 404s. If AllowOverride is ever turned
# on, both addresses work and this line can go back to the .xml form.
Sitemap: https://human.123greetings.com/sitemap.php
